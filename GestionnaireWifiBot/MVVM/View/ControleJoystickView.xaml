<UserControl x:Class="GestionnaireWifiBot.MVVM.View.ControleJoystickView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:GestionnaireWifiBot.MVVM.View"
        xmlns:uc="clr-namespace:GestionnaireWifiBot.User_Controls"
        mc:Ignorable="d">
    <Grid>
        <StackPanel VerticalAlignment="Center">
            <uc:Joystick x:Name="joystick_view" 
                         Xvalue="{Binding X, 
                                UpdateSourceTrigger=PropertyChanged, 
                                Mode=OneWayToSource}"
                         Yvalue="{Binding Y, 
                                UpdateSourceTrigger=PropertyChanged, 
                                Mode=OneWayToSource}"/>
        </StackPanel>
    </Grid>
</UserControl>
